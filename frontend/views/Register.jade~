script(src= "http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js")
script.
	function OpenInNewTab(url) {
		alert(url);
		var win = window.open(url, url);//'_blank'
		win.focus();
	}
	function onAjaxSuccess(data)
	{
	  // Здесь мы получаем данные, отправленные сервером и выводим их на экран.
	  alert(data);
	}
	function send(){
		console.log('Sending');
		$.post('Tournaments', {message:"I am cool!"} , onAjaxSuccess );
	}
html
	h1 != 'Register'
	div#loginbox
			form(name="Register", action="/Register", method="post")
				p.
					Login
				input(type="text", name="login")
				p.
					Pass
				input(type="password", name="password")				
				p.
				input(type="submit", value="Login")
				script.
					//alert('5');
					//document.getElementsByClassName('loginbox');
					//var i = document.getElementsByClassName('loginbox')[0];;//getElementById('loginbox');
					/*var i = document.getElementById('loginbox');
					console.log(i);
					i.innerHTML = '';*/
					//window.open('http://google.com');
					//var tmr = setInterval(send,5000);
					//OpenInNewTab('localhost:3000/Tournaments');

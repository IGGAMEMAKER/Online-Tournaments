//

-var red = "#FF0000";
-var green = "#00FF00";
-var blue = "#0000FF";

include templates/tournaments

mixin games()
	td(style="padding:0 15px 7px 0;")
		Игры:
	td(style="padding:0 15px 7px 0;")
		input(type="checkbox" name="game-pp" value="1" checked)
		| Ping-Pong
	td(style="padding:0 15px 7px 0;")
		input(type="checkbox" name="game-qs" value="2" checked)
		| Quiz
mixin prices()
	td(style="padding:0 15px 7px 0;")
		Стоимость:
	td(style="padding:0 15px 7px 0;")
		input(type="checkbox" name="free" value="1" checked)
		| Бесплатные
	td(style="padding:0 15px 7px 0;")
		select(name="buyIn")
			option(value="0" selected) Все
			option(value="1") до 1$
			option(value="2") 1$-5$
			option(value="3") 5$-15$
			option(value="4") свыше 15$
mixin filterTD()
	td(style="padding:0 15px 7px 0;")

mixin filters()
	center
		#filters.filters(style="border: 0px solid black; width:50%; background: #f0f0f0; padding:25px; margin:20px")
			//style="border: 1px solid #333; border-collapse: separate; border-spacing: 17px 11px; padding:20px"
			h2 Фильтры
			table(style="font-size:22px;")
				tr
					+games()
				tr
					+prices()
				tr 
					td(style="padding:0 15px 7px 0;")
						Акции
					td(style="padding:0 15px 7px 0;")
						input(type="checkbox" name="gifts" value="1" checked)
						| Включить
mixin showTournaments1(message)
	- tournWidth = 3;
	if message
		- for (i=0; i<message.length;i++) {
			if i%tournWidth==0
				tr
					- for (j=0;j<tournWidth;j++){
						if (message[i+j])
							+tournamentTile(message[i+j])
					-}
		- }
html
	head
		title!= 'Tournaments'
		link(rel="stylesheet" type="text/css" href="/css/bootstrap.min.css")
		
	body
		include client
		include templates/tAuth
		+filters()
		table
			+showTournaments1(msg)
		//table
			//tr
				td
					+tournamentTile(msg[0])
				//td
					+tournamentTile()
				//td
					+tournamentTile()
				td
					+tournamentTile1()

		//+showTournaments(msg)
		//script.
			window.onfocus = function(){location.reload(true);}
			//setTimeout(function(){location.reload(true);} , 10000);


html
	head
		title!= title
	body
		- //for (var item in message){
		//p!= message.length
		p!= JSON.stringify(message)
		include tAuth
		br
		br
		h1 List of Tournaments
		table
			tr
				td.
					ID
				td.
					Players
				td.
					Buy In
				td.
					Game Name ID
				td.
					TournamentInfo
			- if (message.length>0){ for (i=0; i<message.length;i++){
				tr
					td
						p!= message[i].tournamentID
					td
						p!= message[i].players+'/'+message[i].goNext[0]
					td
						p!= message[i].buyIn
					td
						p!= message[i].gameNameID
					td
						a(href='TournamentInfo?tID='+message[i].tournamentID)
							Tournament Info
				//p!= message
			- }}



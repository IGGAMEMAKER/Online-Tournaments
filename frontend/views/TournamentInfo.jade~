html
	head
		title!= title
		script(src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js").
	body
		- //for (var item in message){
		p!= message.length
		p!= JSON.stringify(message)
		table
			tr
				td.
					ID
				td.
					Players count
				td.
					Buy In
				td.
					Game Name ID
				td.
					TournamentInfo
			tr
				td
					p!= message[0].tournamentID
				td
					p!= message[0].goNext[0]
				td
					p!= message[0].buyIn
				td
					p!= message[0].gameNameID
				td
					Tournament Info
		script.
			function func(){
				var btn = document.getElementById("reg");
				btn.html = 'UNREG';
				//$.("button.reg").value='UNREGISTER';
				$.ajax({
				  url: 'Alive',
				  method: 'POST',
				  data: { f1:'ololo' },
				  success: function( data ) {
					var msg = JSON.stringify(data);
					alert(msg);
					console.log(msg);
				    //$( "#weather-temp" ).html( "<strong>" + data + "</strong> degrees" );
				  }
				});

				/*jQuery.post('http://localhost:3000/Alive', {f1:'ololo'}, function(data){
					console.log(data);
					alert(JSON.stringify(data));
					alert(1);
				});*/
			}
		button(id="reg" onclick="func()" style="width:100px;height:20px;").
			REGISTER

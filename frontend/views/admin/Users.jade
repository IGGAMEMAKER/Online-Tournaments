//
mixin drawUsers(message)
	table
		tr
			td.
				login
			td.
				money
		- if (message.length>0){ for (i=0; i<message.length;i++){
			tr
				td
					p!= message[i].login
				td
					p!= message[i].money
			//p!= message
		- }}

html
	head
		title!= title
	body
		include ../templates/tAuth
		- console.log('incoming message Browser:');
		- console.log(message);
		- //console.log(JSON.stringify(message));

		- //p!= JSON.stringify(message);
		- var login = session.login;
		- console.log('Browser login =' + login);
		- //if(login) { alert(login); console.log('login = ' + login); }
		
		- //for (var item in message){
		- //p!= message.length
		-//p!= JSON.stringify(message);
		center
			h1 Users
			+drawUsers(msg)
			a(href='Tournaments')
				Show Tournaments
